# CPM Monitor - Dependencias Windows
# pip install -r requirements_win.txt

PyQt5>=5.15.0
requests>=2.28.0
certifi>=2023.0.0

# Audio fingerprinting
sounddevice>=0.4.6
numpy>=1.24.0
pyacoustid>=1.3.0
# pyaudiowpatch>=0.2.12  # alternativa para WASAPI loopback en Windows

# Empaquetado (solo para compilar, no en runtime)
pyinstaller>=6.0.0
